info:
  name: Actuator web endpoint 'caches'_1
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/actuator/caches"
  headers:
    - name: Accept
      value: "*/*"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/actuator/caches"
      method: DELETE
      headers:
        - name: Accept
          value: "*/*"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"
