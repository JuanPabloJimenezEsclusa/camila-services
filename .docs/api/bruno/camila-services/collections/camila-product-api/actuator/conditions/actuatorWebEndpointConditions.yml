info:
  name: Actuator web endpoint 'conditions'
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/actuator/conditions"
  headers:
    - name: Accept
      value: application/vnd.spring-boot.actuator.v3+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/actuator/conditions"
      method: GET
      headers:
        - name: Accept
          value: application/vnd.spring-boot.actuator.v3+json
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
