info:
  name: Actuator web endpoint 'configprops-prefix'
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/actuator/configprops/:prefix"
  headers:
    - name: Accept
      value: application/vnd.spring-boot.actuator.v3+json
  params:
    - name: prefix
      value: <string>
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/actuator/configprops/:prefix"
      method: GET
      headers:
        - name: Accept
          value: application/vnd.spring-boot.actuator.v3+json
      params:
        - name: prefix
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
