info:
  name: Actuator web endpoint 'env-toMatch'
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/actuator/env/:toMatch"
  headers:
    - name: Accept
      value: application/vnd.spring-boot.actuator.v3+json
  params:
    - name: toMatch
      value: <string>
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/actuator/env/:toMatch"
      method: GET
      headers:
        - name: Accept
          value: application/vnd.spring-boot.actuator.v3+json
      params:
        - name: toMatch
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
