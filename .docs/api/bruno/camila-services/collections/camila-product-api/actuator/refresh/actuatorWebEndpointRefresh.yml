info:
  name: Actuator web endpoint 'refresh'
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/actuator/refresh"
  headers:
    - name: Accept
      value: application/vnd.spring-boot.actuator.v3+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/actuator/refresh"
      method: POST
      headers:
        - name: Accept
          value: application/vnd.spring-boot.actuator.v3+json
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
