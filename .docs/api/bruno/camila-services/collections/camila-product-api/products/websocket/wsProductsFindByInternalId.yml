info:
  name: ws/products/findByInternalId
  type: websocket
  seq: 1

websocket:
  url: "{{websocket.baseUrl}}:{{websocket.basePort}}/{{websocket.basePath}}"
  message:
    type: json
    data: |
      { 
        "method": "FIND_BY_INTERNAL_ID",
        "internalId": "45"
      }
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
