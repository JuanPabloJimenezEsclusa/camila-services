# Stop processing code immediately if it encounters an error or warning, instead of continuing to process other parts of the code
config.stopBubbling=true
# Add a special annotation ( @lombok.Generated) to all the code that Lombok generates, so that it's easy to identify which parts of your code were generated by Lombok
lombok.addLombokGeneratedAnnotation=true
