/**
 * This package contains GraphQL adapters for handling product-related queries and mutations. <br><br>
 * These adapters serve as the entry point for GraphQL requests, translating them into use case invocations. <br>
 * They map GraphQL types to domain entities and viceversa, ensuring a clear separation between the API layer and the domain logic. <br>
 * The adapters in this package are responsible for request validation, error handling, and response formatting specific to GraphQL. <br>
 */
@NullMarked
package com.camila.api.product.infrastructure.adapter.input.graphql;

import org.jspecify.annotations.NullMarked;
